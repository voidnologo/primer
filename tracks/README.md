# tracks/

Curated multi-lesson sequences. Empty in v1 — `/primer suggest <goal>` writes here when the learner asks for a track to be persisted.

The skill ships ad-hoc with curriculum-aware suggestions; tracks are an opt-in upgrade for when a learner wants a multi-week plan committed to disk.
