# Topic Index

The map of what's been covered, what's in progress, and what's next-suggested. The Primer reads this at session start to choose suggestions and avoid re-covering ground. Also carries the per-domain depth markers (volatile state, moved here from the profile).

Status legend: `[unexplored]` `[in-progress]` `[covered]` `[mastered]`

---

## Depth markers (confidence + evidence)

One row per top-level domain. `confidence` reflects demonstrated vs self-reported — a marker built only from self-report or a single intake probe stays `low` until a lesson confirms it. See `primer/feedback-protocol.md`.

| Domain | Updated | Depth | Confidence | Evidence |
|---|---|---|---|---|
| <domain> | <date> | <one-line depth> | low | intake probe <date> |

## Open ZPD edges

The Primer's per-session snapshot of where the learner is being stretched. Volatile — updated each session.

- <edge>

---

## <domain>

<one-line domain description>

- `[unexplored]` <topic-slug> — <hook>

---

## Suggested next

The Primer updates this after each session.

1. <next lesson> — <why>
